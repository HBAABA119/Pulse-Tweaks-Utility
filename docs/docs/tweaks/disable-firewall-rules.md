# Disable Firewall Rules

## Overview
- **ID/URL**: `disable-firewall-rules`
- **Description**: Removes all Windows Firewall rules.

## Details

- Removes all custom Windows Firewall rules, restoring default firewall configuration. **Warning:** This may break applications that rely on specific firewall exceptions. Active connections may be interrupted until default rules are established.

