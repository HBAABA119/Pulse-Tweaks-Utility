# Disable Edge HTTP3

## Overview
- **ID/URL**: `disable-edge-http3`
- **Description**: Disables Edge HTTP3 protocol.

## Details

- Disables the HTTP/3 protocol in Microsoft Edge. HTTP/3 is a modern protocol that may cause compatibility issues with some networks. Disabling it forces Edge to use HTTP/2 or HTTP/1.1 for better compatibility.

